# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.115.11
uvicorn==0.34.0
python-multipart==0.0.22

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.48
aiosqlite==0.22.1

# ── Task Queue ────────────────────────────────────────────────────────────────
redis==5.3.1
arq==0.27.0

# ── LLM / Kosong inference layer ─────────────────────────────────────────────
openai>=2.14.0,<2.15.0
anthropic>=0.78.0
google-genai>=1.56.0
mcp>=1,<2
jsonschema>=4.25.1

# ── Vector DB ─────────────────────────────────────────────────────────────────
# 只装客户端 pymilvus,服务端走 docker-compose 的 standalone milvus(image: milvusdb/milvus)
pymilvus==2.5.6

# ── HTTP Client ──────────────────────────────────────────────────────────────
httpx==0.28.1

# ── File I/O ──────────────────────────────────────────────────────────────────
aiofiles==24.1.0
pypdf==6.9.2
python-docx==1.2.0
pypandoc-binary==1.14     # 带预编译 pandoc 二进制,避免容器里再装 apt 包
EbookLib==0.18          # EPUB 读取(解析 OPF/spine 阅读顺序)
beautifulsoup4==4.13.3  # EPUB 章节 XHTML 清洗与正文抽取

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose==3.5.0
bcrypt==5.0.0

# ── Config ────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic>=2.12.5

# ── Logging ───────────────────────────────────────────────────────────────────
loguru>=0.6.0,<0.8

# ── Typing ───────────────────────────────────────────────────────────────────
typing-extensions>=4.15.0

# ── Dev / Test (Optional) ─────────────────────────────────────────────────────
# fpdf2==2.8.3  # 用于创建示例 PDF 文件